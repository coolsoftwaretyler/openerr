{
  "name": "openerr",
  "version": "0.2.0",
  "description": "Colorado Openerr uses the Open States API to tweet about what is happening in Colorado state legislation.",
  "main": "index.js",
  "scripts": {
    "prettier": "./node_modules/.bin/prettier --single-quote --trailing-comma es5 --print-width 80 --write --no-bracket-spacing **/*.js",
    "pretest": "./node_modules/.bin/prettier --single-quote --trailing-comma es5 --print-width 80 --write --no-bracket-spacing **/*.js && eslint --ignore-path .gitignore .",
    "test": "mocha",
    "local": "node index.js local",
    "zip": "zip -r ./openerr.zip *"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ogdenstudios/openerr.git"
  },
  "author": "Ogden Studios",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ogdenstudios/openerr/issues"
  },
  "homepage": "https://github.com/ogdenstudios/openerr#readme",
  "dependencies": {
    "twitter": "^1.7.1"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-config-strongloop": "^2.1.0",
    "mocha": "^6.1.4",
    "prettier": "^1.17.0"
  }
}
